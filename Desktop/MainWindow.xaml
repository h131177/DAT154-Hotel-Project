<Window x:Class="Desktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Desktop"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <TabControl x:Name="tabControl" HorizontalAlignment="Left" Height="400" Margin="0,0,0,0" VerticalAlignment="Top" Width="800">
            <TabItem Header="Magnus">
                <Grid Background="#FFE5E5E5">
                    <ListView Name="studentList" ItemsSource="{Binding}">
                        <ListView.View>
                            <GridView>
                                <GridView.Columns>
                                    <GridViewColumn Width="100" Header="Student" DisplayMemberBinding="{Binding Path=studentname}"></GridViewColumn>
                                    <GridViewColumn Width="100" Header="ID" DisplayMemberBinding="{Binding Path=studentname}"></GridViewColumn>
                                    <GridViewColumn Width="100" Header="Age" DisplayMemberBinding="{Binding Path=studentname}"></GridViewColumn>
                                </GridView.Columns>
                            </GridView>
                        </ListView.View>
                    </ListView>
                </Grid>
            </TabItem>
            <TabItem Header="Andrine">
                <Grid Background="#FFE5E5E5"/>
            </TabItem>
            <TabItem Header="Amalie">
                <Grid Background="#FFE5E5E5"/>
            </TabItem>
            <TabItem Header="Kjetil">
                <Grid Background="#FFE5E5E5"/>
            </TabItem>
        </TabControl>

    </Grid>
</Window>
